/**
 * @import { Builder } from './builder.js';
 */
/**
 * @param {Builder} builder
 * @param {Map<string, string>} metadata
 * @returns {number}
 */
export function encodeMetadata(builder: Builder, metadata: Map<string, string>): number;
import type { Builder } from './builder.js';
