/**
 * @param {Builder} builder
 * @param {Schema} schema
 * @returns {number}
 */
export function encodeSchema(builder: Builder, schema: Schema): number;
import type { Builder } from './builder.js';
import type { Schema } from '../types.js';
