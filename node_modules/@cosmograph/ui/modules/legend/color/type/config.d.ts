import { DisplayStateConfigInterface } from '../../../_abstract/display-state-manager';
export type TypeColorLegendItem = {
    /**
     * The color value to display in the legend.
     */
    color: string;
    /**
     * The label text to display next to the color in the legend.
     */
    label: string;
};
export declare const defaultMaxDisplayedItems = 15;
export interface TypeColorLegendConfigInterface extends DisplayStateConfigInterface {
    /**
     * Whether to hide the legend.
     * Default: `false`
     */
    hidden?: boolean;
    /**
     * The label for the color legend.
     * @default undefined
     */
    label?: string;
    /**
     * An array of color legend items, where each item has a color and a label.
     * These items will be displayed in the legend, with each item showing its color and label.
     * @default []
     */
    items?: TypeColorLegendItem[];
    /**
     * Whether to align the color legend items to the right.
     * When true, the legend items will be right-aligned within their container.
     * @default false
     */
    alignRight?: boolean;
    /**
     * The maximum number of color legend items to display.
     * When exceeded, a "show more" button will appear to display additional items.
     * @default 15
     */
    maxDisplayedItems?: number;
    /**
     * The label formatter function to format the legend labels.
     * @param n - The label string to format
     * @returns The formatted label string
     */
    labelFormatter?: (n: string) => string;
    /**
     * Whether to enable event handling for the color legend.
     * When true, click and hover events will be active on legend items.
     * @default true
     */
    enableEvents?: boolean;
    /**
     * Whether the color legend list (if "show more" is active) is expanded or not.
     * @default false
     */
    expanded?: boolean;
    /**
     * Callback function that is called when a color legend item is clicked.
     * @param e - The click event object
     * @param item - The color legend item that was clicked, or undefined if no item was clicked
     */
    onClick?: (e: Event, item?: TypeColorLegendItem) => void;
    /**
     * Callback function that is called when the mouse hovers over a color legend item.
     * @param e - The hover event object
     * @param item - The color legend item that was hovered over, or undefined if no item is being hovered
     */
    onHover?: (e: Event, item?: TypeColorLegendItem) => void;
    /**
     * Callback function that is called when the "show more" button is clicked.
     * @param expanded - Whether the list is expanded or collapsed
     */
    onShowMoreClick?: (expanded: boolean) => void;
}
export declare const defaultTypeColorLegendConfig: TypeColorLegendConfigInterface;
