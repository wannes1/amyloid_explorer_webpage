import { TypeColorLegendConfigInterface, TypeColorLegendItem } from './config';
import { DisplayStateManager } from '../../../_abstract/display-state-manager';
/**
 * Represents a TypeColorLegend component that displays a legend for color types.
 * The legend can be configured with various options, such as the list of items to display,
 * the maximum number of items to display, and event handlers for item clicks and hovers.
 * The legend can be expanded or collapsed to show more or fewer items.
 */
export declare class TypeColorLegend extends DisplayStateManager {
    private _containerNode;
    private _config;
    private _scrollContainer;
    private _legendItems;
    private _showMoreButton;
    private _wrapper;
    private _itemHeight;
    private _itemMargin;
    private _visibleItemsCount;
    private _startIndex;
    private _endIndex;
    private _listItems;
    private _selectedItem?;
    private _isExpanded;
    private _isHidden;
    private _calculatedWidth;
    private _maxMeasuredWidth;
    private _maxMeasuredCharLength;
    private _label;
    /**
     * Constructs a new TypeColorLegend instance with the provided container node and optional configuration.
     *
     * @param containerNode - The HTML element that will contain the legend.
     * @param config - Optional configuration object for the legend.
     */
    constructor(containerNode: HTMLElement, config?: TypeColorLegendConfigInterface);
    /**
     * Gets the configuration object for the TypeColorLegend instance.
     */
    get config(): TypeColorLegendConfigInterface;
    /**
     * Gets the HTML element that contains the legend items.
     */
    get legendItems(): HTMLElement;
    /**
     * Gets the expanded state of the TypeColorLegend instance.
     */
    get isExpanded(): boolean;
    /**
     * Sets the configuration for the TypeColorLegend instance.
     *
     * @param config - Optional configuration object for the legend. If not provided, the existing configuration will be used.
     */
    setConfig(config?: TypeColorLegendConfigInterface): void;
    /**
     * Sets the selected item in the TypeColorLegend instance.
     *
     * @param item - The TypeColorLegendItem to set as the selected item, or undefined to clear the selection.
     */
    setSelectedItem(item?: TypeColorLegendItem): void;
    /**
     * Resets the scroll position of the legend container to the top and clears the selected item.
     * This updates the visible items in the legend.
     */
    resetList(): void;
    /**
     * Gets the configuration object for the TypeColorLegend instance.
     *
     * @returns The configuration object for the TypeColorLegend instance.
     */
    getConfig(): TypeColorLegendConfigInterface;
    /**
     * Shows the legend container by removing the 'disabled' class and adding the 'enabled' class to the scroll container.
     */
    show(): void;
    /**
     * Hides the legend container by removing the 'enabled' class and adding the 'disabled' class to the scroll container.
     */
    hide(): void;
    /**
     * Destroys the TypeColorLegend instance by removing the scroll event listener and clearing the container node's HTML.
     */
    destroy(): void;
    /**
     * Sets the text content of the fallback div to 'Data loading...' to indicate that data is currently being loaded.
     */
    setLoadingState(): void;
    private _updateLegend;
    private _updateVisibleItems;
    private _createElements;
    private _createShowMoreButton;
    private _handleShowMore;
    private _handleShowMoreCount;
    private _updateShowMoreButton;
    private _calculateItemDimensions;
    private _createLegendItem;
    private _handleScroll;
    private _calculateOptimalWidth;
    private _applyCalculatedWidth;
}
export type { TypeColorLegendConfigInterface, TypeColorLegendItem };
