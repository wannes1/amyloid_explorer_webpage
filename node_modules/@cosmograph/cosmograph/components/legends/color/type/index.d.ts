import { TypeColorLegend, TypeColorLegendConfigInterface, TypeColorLegendItem } from '@cosmograph/ui';
import { CosmographData } from "../../../../cosmograph/config/interfaces";
import { CosmographExclusiveSelectionComponent } from "../../../_abstract/exclusive";
import { CosmographTypeColorLegendConfig } from './config';
/**
 * Represents a color legend for the Cosmograph visualization, which displays the available types or categories of data points or links.
 */
export declare class CosmographTypeColorLegend extends CosmographExclusiveSelectionComponent<CosmographTypeColorLegendConfig, TypeColorLegend, TypeColorLegendItem> {
    private _uiComponentConfig;
    private _linkColorUpdateHandler;
    private _pointColorUpdateHandler;
    private _currentColorAccessor?;
    private _fetchDataPromise;
    private get _summary();
    private get _isLinkDirectionStrategy();
    get defaultConfig(): CosmographTypeColorLegendConfig;
    get accessor(): string | undefined;
    private get _hasClickableEvents();
    protected onInitialize(): void;
    protected createUIComponent(targetElement: HTMLElement): TypeColorLegend;
    protected createComponentConfig(config?: CosmographTypeColorLegendConfig): TypeColorLegendConfigInterface;
    protected compareSelectedItem(item?: TypeColorLegendItem): boolean;
    protected setSelectedItem(item?: TypeColorLegendItem): void;
    protected applyFilter(item?: TypeColorLegendItem): void;
    protected fetchData(): Promise<void>;
    protected onConfigUpdate(): Promise<void>;
    protected getCurrentData(): Promise<CosmographData | undefined>;
    /** @hidden **/
    protected shouldUpdateComponentData(): Promise<boolean>;
    private _onColorUpdated;
    private _getDistinctValues;
    private _checkEvents;
    private _updateLegend;
    private _updateItems;
    private _createLegendItems;
    private _getMappingFunction;
    /**
     * Shows the `CosmographTypeColorLegend`.
     */
    show(): void;
    /**
     * Hides the `CosmographTypeColorLegend`.
     */
    hide(): void;
    /**
     * Removes the `CosmographTypeColorLegend` instance by destroying it.
     */
    remove(): void;
}
export type { CosmographTypeColorLegendConfig };
//# sourceMappingURL=index.d.ts.map