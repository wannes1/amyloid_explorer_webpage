import { TypeColorLegendConfigInterface, TypeColorLegendItem } from '@cosmograph/ui';
export interface CosmographTypeColorLegendConfigInterface {
    /**
     * Whether to use the data from the links instead of the points for the type legend.
     * @default `false`
     */
    useLinksData?: boolean;
    /**
     * Whether to select the points/links with the corresponding values when clicking on the legend items.
     * @default `true`
     */
    selectOnClick?: boolean;
    /**
     * An optional array of `TypeColorLegendItem` objects that will override the default legend items.
     */
    overrideItems?: TypeColorLegendItem[];
    /**
     * The field to sort the legend items by. Should be from the same table. If not provided, the items will not be sorted.
     */
    sortBy?: string;
    /**
     * The order to sort the legend items in. Can be either 'asc' for ascending order or 'desc' for descending order.
     * If not provided, the items will not be sorted.
     */
    sortOrder?: 'asc' | 'desc';
    /**
     * Whether to hide legend items for unknown values.
     * @default `true`
     */
    hideUnknown?: boolean;
    /**
     * When the legend collapses ("show less" is clicked), determines whether to deselect an item
     * if it was selected from the expanded list but is now hidden.
     * @remarks Has no effect if `selectOnClick` is `false`.
     * @default `true`
     */
    resetSelectionOnCollapse?: boolean;
    /**
     * Preserves the selection on unmount.
     *
     * @default false
     */
    preserveSelectionOnUnmount?: boolean;
    /**
     * Controls the visibility of the legend items' labels.
     * @default `false`
     */
    showLabel?: boolean;
    /**
     * The label for the legend. Can be a string or a function
     that returns a string based on an accessor.
     */
    labelResolver?: string | ((accessor?: string) => string);
}
export declare const defaultCosmographTypeColorLegendConfig: CosmographTypeColorLegendConfig;
export interface CosmographTypeColorLegendConfig extends CosmographTypeColorLegendConfigInterface, Omit<TypeColorLegendConfigInterface, 'items' | 'label'> {
}
//# sourceMappingURL=config.d.ts.map